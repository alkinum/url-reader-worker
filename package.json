{
  "name": "url-reader-worker",
  "version": "0.3.2",
  "author": {
    "name": "BackRunner",
    "url": "https://github.com/backrunner"
  },
  "scripts": {
    "build": "rimraf ./dist && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "0.0.7",
    "@cloudflare/workers-types": "^4.20240620.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.5.2",
    "wrangler": "^3.61.0"
  },
  "dependencies": {
    "@backrunner/turndown": "^7.2.3",
    "@mozilla/readability": "^0.5.0",
    "cheerio": "^1.0.0-rc.12",
    "cookie-es": "^1.1.0",
    "puppeteer-afp": "^1.1.6",
    "unpdf": "^0.10.1"
  }
}
